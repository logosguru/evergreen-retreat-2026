-- =====================================================================
-- 일정 관리자 전용 필드: 담당자(owner) · 비고(admin_note)
-- 공개 화면엔 노출하지 않음 (공개 쿼리에서 컬럼 미선택 + 화면 미표시).
-- 다국어 불필요(관리자 내부용) — 단일 텍스트 컬럼.
-- RLS 변경 없음 (기존 테이블 단위 공개읽기/관리자쓰기 정책 유지).
-- =====================================================================

alter table public.schedule_items
  add column owner      text,
  add column admin_note text;
